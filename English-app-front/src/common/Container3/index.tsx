import { StyledContainer3} from "./styles";
import {ContainerProps, ContainerProps2} from "../types";

const Container3 = ({ children }: ContainerProps2) => (
  <StyledContainer3>{children}</StyledContainer3>
);

export default Container3;
