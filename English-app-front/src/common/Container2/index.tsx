import { StyledContainer2 } from "./styles";
import {ContainerProps, ContainerProps2} from "../types";

const Container2 = ({ children }: ContainerProps2) => (
  <StyledContainer2>{children}</StyledContainer2>
);

export default Container2;
